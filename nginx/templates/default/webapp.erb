upstream myapp1 {
             <%   @hosts.each do |host| -%>   
                server <%= host %>:<%= @webapp_port %>;
 	     <% end -%>
        }

        server {
                listen <%= @privateip %>:<%= @listening_port %>;

                location / {
                        proxy_pass http://myapp1;
                }
        }

